# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: pipx
    paths:
      confdir: '.local/pipx'
      conffile: 'config'
      xdg_dirname: 'pipx'
      xdg_conffile: 'config'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
